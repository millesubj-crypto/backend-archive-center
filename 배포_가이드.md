# Archive Center 외부 서버 배포 가이드

폰에서 RisuAI를 쓸 때 백엔드를 외부 서버(Railway)에 올리는 방법입니다.
무료로 시작할 수 있고, GitHub 계정만 있으면 됩니다.

---

## 준비물

- GitHub 계정 (없으면 https://github.com 에서 무료 가입)
- Railway 계정 (https://railway.app — GitHub으로 로그인)
- LLM API 키 (OpenAI, Claude API 등)

---

## 1단계: GitHub에 코드 올리기

### 폴더 구조 준비

아래처럼 하나의 폴더를 만드세요:

```
archive-center-server/        ← 이 폴더를 GitHub에 올릴 것
├── Procfile                  ← (이 패키지에 포함)
├── railway.toml              ← (이 패키지에 포함)
├── nixpacks.toml             ← (이 패키지에 포함)
├── requirements.txt          ← (이 패키지에 포함)
├── runtime.txt               ← (이 패키지에 포함)
├── .gitignore                ← (이 패키지에 포함)
└── backend/                  ← Archive_Center_1_0.zip 안의 backend 폴더 그대로
    ├── main.py
    ├── settings.py
    ├── database.py
    ├── models.py
    ├── requirements.txt
    ├── .env.example
    └── (나머지 파일들...)
```

### 이 패키지의 파일들 + backend 폴더를 하나로 합치세요

1. 새 폴더 `archive-center-server` 만들기
2. 이 배포 패키지의 파일들(Procfile, railway.toml, nixpacks.toml, requirements.txt, runtime.txt, .gitignore)을 넣기
3. `Archive_Center_1_0.zip`에서 `backend` 폴더 꺼내서 같이 넣기

### GitHub 저장소 만들기

```bash
cd archive-center-server
git init
git add .
git commit -m "Archive Center backend"
```

GitHub에서 새 저장소(repository) 만들고:
```bash
git remote add origin https://github.com/내아이디/archive-center-server.git
git push -u origin main
```

> **⚠️ backend/.env 파일은 절대 커밋하지 마세요** (.gitignore에 이미 포함됨)

---

## 2단계: Railway에 배포하기

1. https://railway.app 접속 → GitHub으로 로그인
2. **New Project** → **Deploy from GitHub repo** 클릭
3. 방금 만든 `archive-center-server` 저장소 선택
4. Railway가 자동으로 빌드 시작 (2~3분 소요)

---

## 3단계: 환경변수(API 키) 설정

Railway 대시보드에서 내 프로젝트 → **Variables** 탭 → 아래 변수들 추가:

| 변수명 | 값 | 설명 |
|--------|-----|------|
| `DATABASE_URL` | `sqlite:////data/memory.db` | DB 저장 경로 |
| `CHROMA_SHADOW_PERSIST_DIRECTORY` | `/data/.chroma_shadow` | ChromaDB 경로 |
| `PROJECT_MAIN_PROVIDER` | `openai` | Main LLM 제공사 |
| `PROJECT_MAIN_ENDPOINT` | `https://api.openai.com/v1` | Main LLM 주소 |
| `PROJECT_MAIN_API_KEY` | `sk-...` | **본인 API 키** |
| `PROJECT_MAIN_MODEL` | `gpt-4o` | 사용할 모델 |
| `PROJECT_SUPERVISOR_ENDPOINT` | `https://api.openai.com/v1` | Supervisor LLM |
| `PROJECT_SUPERVISOR_API_KEY` | `sk-...` | **본인 API 키** |
| `PROJECT_SUPERVISOR_MODEL` | `gpt-4o` | Supervisor 모델 |
| `PROJECT_CRITIC_PROVIDER` | `openai` | Critic LLM 제공사 |
| `PROJECT_CRITIC_ENDPOINT` | `https://api.openai.com/v1` | Critic LLM |
| `PROJECT_CRITIC_API_KEY` | `sk-...` | **본인 API 키** |
| `PROJECT_CRITIC_MODEL` | `gpt-4o` | Critic 모델 |
| `PROJECT_EMBEDDING_PROVIDER` | `openai` | 임베딩 제공사 |
| `PROJECT_EMBEDDING_ENDPOINT` | `https://api.openai.com/v1` | 임베딩 주소 |
| `PROJECT_EMBEDDING_API_KEY` | `sk-...` | **본인 API 키** |
| `PROJECT_EMBEDDING_MODEL` | `text-embedding-3-small` | 임베딩 모델 |
| `ALLOWED_ORIGINS` | `*` | CORS (모든 출처 허용) |
| `TOP_K_RESULTS` | `5` | 검색 결과 수 |
| `SUPERVISOR_TIMEOUT` | `30` | 타임아웃(초) |
| `CRITIC_TIMEOUT` | `30` | 타임아웃(초) |
| `MAIN_TIMEOUT` | `60` | 타임아웃(초) |

변수 다 넣으면 Railway가 자동으로 재배포합니다.

### Railway 영구 볼륨 설정 (데이터 유지용)

Railway 무료 플랜은 재배포 시 파일이 초기화됩니다.
데이터를 유지하려면:

1. 프로젝트 → **Volumes** → **New Volume** 클릭
2. Mount Path: `/data` 입력
3. Add 클릭

이렇게 하면 `DATABASE_URL`을 `sqlite:////data/memory.db`로 설정했을 때 데이터가 영구 보존됩니다.

---

## 4단계: 서버 주소 확인

Railway 대시보드 → **Settings** → **Domains**에서 주소 확인:

```
https://archive-center-server-xxxx.railway.app
```

헬스체크로 동작 확인:
```
https://archive-center-server-xxxx.railway.app/health
```
브라우저에서 열었을 때 `{"status":"ok"}` 같은 응답이 오면 성공입니다.

---

## 5단계: RisuAI 플러그인 설정

1. RisuAI 앱(폰)에서 Archive Center 플러그인 설정 열기
2. **Bridge URL** 항목을 찾아서 변경:
   - 기존: `http://localhost:8000`
   - 변경: `https://archive-center-server-xxxx.railway.app`
3. 저장 후 연결 테스트

---

## OpenAI 말고 다른 API 쓰고 싶다면

### Claude API (Anthropic) 사용 시
```
PROJECT_MAIN_PROVIDER=anthropic
PROJECT_MAIN_ENDPOINT=https://api.anthropic.com/v1
PROJECT_MAIN_API_KEY=sk-ant-...
PROJECT_MAIN_MODEL=claude-sonnet-4-20250514
```

### 로컬 Ollama를 ngrok으로 터널링해서 쓸 때
```
PROJECT_MAIN_ENDPOINT=https://xxxx.ngrok-free.app/v1
PROJECT_MAIN_API_KEY=ollama
PROJECT_MAIN_MODEL=llama3
```

---

## 문제 해결

| 증상 | 원인 | 해결 |
|------|------|------|
| 빌드 실패 | requirements 문제 | Railway Logs 탭에서 오류 확인 |
| /health 응답 없음 | 앱 시작 실패 | 환경변수 누락 여부 확인 |
| 연결은 되는데 기능 안됨 | API 키 오류 | Railway Variables에서 키 재확인 |
| 데이터가 사라짐 | 볼륨 미설정 | Volumes 탭에서 /data 볼륨 추가 |

---

## 비용

- Railway 무료 플랜: 월 $5 크레딧 제공 (소규모 사용 시 충분)
- LLM API 비용: 사용량에 따라 별도 청구 (OpenAI 등)
- 더 저렴하게 하려면: Embedding을 `text-embedding-3-small`로 유지하고 Main/Critic/Supervisor를 `gpt-4o-mini`로 변경
